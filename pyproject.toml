[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "smoke-strategy-lab"
version = "0.1.0"
description = "Clean isolated crypto strategy research lab"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
packages = ["strategy_lab"]

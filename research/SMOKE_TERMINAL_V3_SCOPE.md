# SMOKE TERMINAL V3 SCOPE

Presentation and causal chart transport only. No strategy semantics, profitability logic, P8 freeze contract, paper decision rules or exchange execution are changed.

The shared deterministic chart transport helper is also present on the live-paper base branch solely to keep the UI PR conflict-free; it is unused until the V3 entrypoint is selected.

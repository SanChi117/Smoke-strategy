# Architecture

`smoke_control_server_v3.py` applies V2 hardening, replaces only the HTTP handler, serves the new terminal, and exposes deterministic read-only chart endpoints. Strategy code remains untouched.
